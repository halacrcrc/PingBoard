{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "PingBoard 默认权限：核心能力 + 文件保存对话框 + 修改窗口标题",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-set-title",
    "dialog:default"
  ]
}
